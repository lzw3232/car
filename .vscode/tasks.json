{
  "tasks": [
    {
      "type": "shell",
      "label": "clang++ build active file",
      "command": "g++",
      "args": [
        "-g",
        "${file}",
        "-o",
        //"${fileDirname}/${fileBasenameNoExtension}",
        "${workspaceFolder}/main.cpp"
      ],
      "options": {
        "cwd": "/usr/bin"
      }
    }
  ],
  "version": "2.0.0"
}